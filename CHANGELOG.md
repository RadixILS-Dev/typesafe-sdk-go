# Changelog

All notable changes to this module are documented here. The format follows
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project aims
to follow [semantic versioning](https://semver.org/). The `Version` constant in
`config.go` and the `vX.Y.Z` git tag are kept in step; CI checks the match.

While the version is `0.x`, exported APIs may change in a minor release.

## [Unreleased]

Nothing yet.

## [0.1.0] - unreleased

Initial release of the Go client for the TypeSafe AI System One API. Set the
release date and push the `v0.1.0` tag when cutting it; the links below resolve
once that tag exists.

[Unreleased]: https://github.com/RadixILS-Dev/typesafe-sdk-go/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/RadixILS-Dev/typesafe-sdk-go/releases/tag/v0.1.0
